console.log("inside product-batch js file")

// How do we access the liquid assets ie. {{product.id}}
// var xhr = new XMLHttpRequest();
// xhr.open('GET', 'https://2aab22d7.ngrok.io/batches');
// xhr.onload = function() {
//     if (xhr.status === 200) {
//         console.log(xhr.responseText);
//     }
//     else {
//         alert('Request failed.  Returned status of ' + xhr.status);
//     }
// };
// xhr.send();

b = <%= Batch.first.to_json %>

var heading = document.createElement("h1")

heading.innerHTML = preOrderFactory.getAttribute("data-id") ;
heading.innerHTML += " IS the product ID for this product!!!" ;
heading.style.color = "red"
heading.style.border = "10px solid purple"
var aboveProductTitle = document.querySelector('div.product-single')
// preOrderFactory.append(heading);
aboveProductTitle.prepend(heading);
