console.log("Inside product-batch js file");

var heading = document.createElement('h1');
var aboveProductTitle = document.querySelector('div.product-single');

heading.innerHTML = preOrderFactory.getAttribute('data-id');
heading.innerHTML += " is the id for this product.";
aboveProductTitle.prepend(heading);
